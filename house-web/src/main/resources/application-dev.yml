
server:
  port: 8080
  servlet:
    servlet:
      context-path:/
spring:
  datasource:
    username: root
    password:
    url: jdbc:mysql://localhost:3306/db_house?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com.eric.house.dao.model


